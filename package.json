{
  "name": "e-hotel-root",
  "version": "1.0.0",
  "description": "Root management for E-Hotel Project",
  "private": true,
  "scripts": {
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "npm run server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "lucide-react": "^0.577.0"
  }
}
